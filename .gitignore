build/
dist/
target/
__pycache__/
*.bin
*.cue

# id Software's shareware data never belongs here. It lives in the Quake tree
# that builds the image, and this repo only ever holds hashes of the result.
*.PAK
*.pak
quake106.zip

# Local planning and knowledge-graph output, never shipped.
PLAN.md
SESSION.md
SECOND_PASS_CONSOLIDATION_PLAN.md
graphify-out/

# Retained local pre-split build inputs; no longer release dependencies.
/games/PSoXide-runtime/
/games/PSoXide-cortex-current/
