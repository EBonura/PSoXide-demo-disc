[package]
name    = "disc-toc"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-or-later"

# Shared by the host disc builder and the no_std launcher, so it belongs to
# neither workspace.
[workspace]

[lib]
path = "src/lib.rs"

[dependencies]
