# `trim-paths` is a nightly cargo feature; these crates are nightly-pinned
# anyway (bare-metal MIPS needs build-std), so the gate costs nothing.
cargo-features = ["trim-paths"]

[package]
name    = "loader"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-or-later"

# Standalone: this crate links at its own base address with its own linker
# script, so it must not join any workspace.
[workspace]

[[bin]]
name = "loader"
path = "src/main.rs"

[profile.release]
# Keep absolute host paths out of the shipped binary. rustc records the
# source path of every panic/assert site (core::panic::Location) as a
# string in .rodata, which on the PS1 means the developer's home directory
# occupies console RAM and ships on the public disc image.
trim-paths    = "all"
panic         = "abort"
codegen-units = 1
lto           = true
opt-level     = "s"

[dependencies]
carousel = { path = "../carousel" }
psx-pack = { path = "../games/PSoXide-runtime/sdk/crates/psx-pack" }
psx-io   = { path = "../games/PSoXide-runtime/sdk/crates/psx-io" }
psx-math = { path = "../games/PSoXide-runtime/sdk/crates/psx-math" }
# Data only: the checklist borrows the SDK's 8x8 font table and draws it
# with GP0 rectangles itself, so none of psx-font's VRAM code is linked.
psx-font = { path = "../games/PSoXide-runtime/sdk/crates/psx-font" }
